<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AutoBuyActivity" android:windowSoftInputMode="adjustPan"></activity>
        <activity
            android:name=".EditBasketNamePopActivity"
            android:theme="@style/AppTheme.PopMe"></activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".MyAccountBasketsActivity" android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".BasketActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>